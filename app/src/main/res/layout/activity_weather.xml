<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/weatherDebugLabel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/weatherWeatherFragment"
        android:text="WEATHER ACTIVITY"
        android:textColor="#000000"
        />

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/weatherWeatherFragment"
        android:name="com.lifestyle.weather.WeatherFragment"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/weatherDebugLabel"
        tools:layout="@layout/fragment_weather" />

</androidx.constraintlayout.widget.ConstraintLayout>