<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/profileDebugLabel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/profileProfileFragment"
        android:text="PROFILE ACTIVITY"
        android:textColor="#000000"
        />

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/profileProfileFragment"
        android:name="com.lifestyle.profile.ProfileFragment"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/profileDebugLabel"
        tools:layout="@layout/fragment_profile" />

</androidx.constraintlayout.widget.ConstraintLayout>